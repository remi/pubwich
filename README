-------------------------------------------------------------
 
   Pubwich
   $Date$
  
   Aggrégation du contenu publié sur différents
   services Web.

   Rémi Prévost (http://remiprevost.com)
 
-------------------------------------------------------------


NOTE
-------------------------------------------------------------
Ce fichier est en cours d'écriture.


COMPOSANTS REQUIS
-------------------------------------------------------------
Les composants suivants doivent être installés pour que
Pubwich fonctionne :

- Apache
- PHP 5 (avec les extensions suivantes : SimpleXML et cURL)
- PEAR (requis pour la librairie Cache_Lite)
- crontab 


INSTALLATION
-------------------------------------------------------------

1. Dupliquer `cfg/config.sample.php` vers `cfg/config.php`

   (optionnel : Si vous voulez utiliser un thème
   personnalisé, dupliquer le dossier `themes/default` vers
   `themes/nom_du_theme` et modifier la constante `PUBWICH_THEME`
   dans le fichier `cfg/config.php` pour "nom_du_theme".
   
2. Modifier le nouveau fichier `config.php` en remplaçant
   les espaces vides par vos informations (clés d'API, nom
   d'utilisateur, URL du site, etc.) et en modifiant le tableau passé
   en argument à Pubwich::setServices().

3. Modifier votre fichier `crontab` (en exécutant
   `crontab -e`) et ajouter la ligne suivante :

   */<N> * * * * <PHP> -f <CHEMIN_ABSOLU>/cron/cron.php

   En remplacant les éléments suivants :

   <N>             L'interval (en minutes) de rafraichissement du cache
   <PHP>           Le chemin vers PHP 5 (habituellement `/usr/bin/php`)
   <CHEMIN_ABSOLU> Le chemin absolu vers l'installation de Pubwich

   Exemple:

   */10 * * * * /usr/bin/php -f /home/utilisateur/public_html/pubwich/trunk/cron/cron.php

4. Changer les permissions du dossier `cache/` pour qu'il
   soit "writeable" par tous (`$ chmod 0777 cache/`).

5. Tout devrait fonctionner (en navigant vers le fichier `index.php` placé sur un serveur!)


À FAIRE
-------------------------------------------------------------

[x] Meilleure gestion de la récupération des données
    externes (dépendante de cURL présentement) via une
    classe dédiée qui offre plusieurs alternatives. 

[x] Meilleur système de  configuration (ne pas avoir à
    saisir les URLS complètes, seulement les paramètres uniques).
    Les passer lorsqu'on créé l'objet de service, via init().

[x] Utilisation d'une classe statique `Pubwich` pour éviter
    de polluer le namespace global avec des fonctions
    utilitaires.

[x] Support de thèmes

[x] Plus de services (Facebook, RSS générique, etc.)

[x] Meilleur système de configuration de service pour
    permettre l'utilisation multiple d'un même service
    avec des configurations différentes (comme RSS générique)

[x] Décider d'un meilleur système de configuration/template. Est-ce que
    lorsqu'on active un service, il se rajoute automatique dans le
    template? Est-ce qu'on laisse le contrôle total sur le "markup"
    à l'utilisateur?

[x] Créer des répertoires `trunk`, `branches` et `tags` dans le dépot.

[x] Apprendre à utiliser les "branches"

[ ] Permettre une plus grande personnalisation des services (eg. types de
    statistiques pour Last.fm, photos de groupes pour Flickr, etc.)

